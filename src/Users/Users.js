import React, { PureComponent } from 'react';
import { directive } from '@babel/types';

class Users extends PureComponent{
    render(){
        return(
            <div>
                this is Users folder that contain the users data...
            </div>
        )
    }
}
export default Users;